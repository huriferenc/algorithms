const insertingSort = require('./insertingSort');

const list = [2, 4, 5, 3, 8, 9, 7, 1];
console.log(list);

insertingSort(list);
console.log(list);

const list2 = [9, 8, 7, 6, 5, 4, 3, 2, 1];
console.log(list2);

insertingSort(list2);
console.log(list2);
